import ProjectsList from "@/components/projects/ProjectsList";

type Props = {};

const page = ({}: Props) => {
  return (
    <div className="py-5">
      <ProjectsList />
    </div>
  );
};

export default page;
